{
  "name": "eyymi-backend-convex",
  "version": "1.0.0",
  "description": "EYYMI Convex Backend",
  "scripts": {
    "dev": "convex dev",
    "codegen": "convex codegen",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "vitest run convex/__tests__/e2e.test.ts",
    "typecheck": "tsc --noEmit -p convex/tsconfig.json"
  },
  "dependencies": {
    "convex": "^1.32.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
